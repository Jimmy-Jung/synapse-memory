---
description: 환경 진단 — apfel / macOS 버전 / Apple Silicon / vault 경로 / Claude Code CLI 가용성 체크
---

!`synapse-memory doctor`

위 출력은 synapse-memory 동작에 필요한 외부 의존성(apfel, Claude Code CLI, vault 경로, Python 버전)의 가용성을 정리한 결과입니다. ✗ 표시된 항목이 있으면 해결 방법을 함께 안내하고, 자동 복구 가능한 항목이면 `/sm:fix` 또는 `synapse-memory doctor --fix`를 제안하세요. 모두 ✓이면 다음 단계(`/sm:daily` 또는 `/sm:ask`)를 제안하세요.
