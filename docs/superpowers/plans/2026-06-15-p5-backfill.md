# P5 — 초기 백필 (빈 vault 재구축, 재개 가능) Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: superpowers:subagent-driven-development 또는 executing-plans. TDD, 태스크별 커밋.

**Goal:** 빈 vault에서 전체 raw 대화 이력을 한 번 훑어 wiki를 처음부터 구축한다(R5). P1 `ingest_source`를 **재개 가능한 청크(batch)**로 반복 호출하고, 중단되면 이어서 진행한다.

**Architecture:** 백필 = 빈 워터마크 상태의 첫 ingest. `ingest_source`에 per-doc 체크포인트(`checkpoint_each`)를 더해 중단-재개를 보장하고, `run_backfill`이 배치 단위로 소진될 때까지 반복한다. 유휴(min_age) 필터는 끈다(과거 이력은 전부 settled). 비용·중단은 batch_size/max_batches로 통제.

**Tech Stack:** P1 `wiki/ingest.py`(`ingest_source`/`IngestResult`), `wiki/watermark.py`, `wiki/log.py`, `pytest`.

---

## 범위 메모

- **포함**: per-doc 체크포인트(재개), 배치 백필 루프, CLI `backfill`.
- **제외**: 멀티소스(claude-code 단일), 마이그레이션 변환기(R5: 기존 카드 폐기, 변환 안 함).
- **재개**: 워터마크가 doc마다 전진 → 중단 후 재실행하면 남은 것부터.
- **테스트 격리**: `ingest_source`/LLM 주입·mock으로 결정적. 실제 LLM 호출 없음.

---

## File Structure

- Modify: `src/synapse_memory/wiki/ingest.py` — `ingest_source(..., checkpoint_each=False)`.
- Create: `src/synapse_memory/wiki/backfill.py` — `run_backfill`, `BackfillResult`.
- Modify: `src/synapse_memory/cli.py` — `backfill` 서브커맨드.
- Modify: `src/synapse_memory/wiki/__init__.py` — export.
- Test: `tests/test_wiki_ingest_checkpoint.py`, `tests/test_wiki_backfill.py`, `tests/test_cli_backfill.py`.

---

## Task 1: per-doc 체크포인트 (재개 가능)

**Files:** Modify `src/synapse_memory/wiki/ingest.py`; Test `tests/test_wiki_ingest_checkpoint.py`.

- [ ] **Step 1: failing test**
```python
# tests/test_wiki_ingest_checkpoint.py
"""checkpoint_each=True면 doc마다 watermark 전진 → 중단-재개."""
from __future__ import annotations

import json
import os
from pathlib import Path

import synapse_memory.wiki.ingest as ingest_mod
from synapse_memory.wiki.ingest import ingest_source
from synapse_memory.wiki.watermark import load_watermark


def _sess(root: Path, name: str, text: str, mtime: float) -> None:
    f = root / f"{name}.jsonl"
    f.parent.mkdir(parents=True, exist_ok=True)
    f.write_text(json.dumps({"message": {"role": "user", "content": text}}) + "\n", encoding="utf-8")
    os.utime(f, (mtime, mtime))


def test_checkpoint_advances_watermark_midway(tmp_path, monkeypatch) -> None:
    root = tmp_path / "raw" / "claude-code"
    _sess(root, "a", "first", 1_700_000_000)
    _sess(root, "b", "second", 1_700_000_100)
    state = tmp_path / "state.json"
    calls = {"n": 0}

    def flaky(prompt, *, system=None, model=None, json_schema=None, env=None, timeout=120, **kw):
        calls["n"] += 1
        if calls["n"] == 2:
            raise RuntimeError("interrupted")
        return {"operations": [{"op": "create", "type": "concept",
                "slug": f"c{calls['n']}", "title": "C", "body": "b"}]}

    monkeypatch.setattr(ingest_mod.ai_api, "complete_structured", flaky)
    ingest_source("claude-code", vault_path=tmp_path, raw_root=root,
                  watermark_path=state, ai_env=None, today="2026-06-15", checkpoint_each=True)
    assert load_watermark("claude-code", path=state) is not None  # 첫 doc 후 체크포인트


def test_without_checkpoint_saves_once_at_end(tmp_path, monkeypatch) -> None:
    root = tmp_path / "raw" / "claude-code"
    _sess(root, "a", "x", 1_700_000_000)
    state = tmp_path / "state.json"
    monkeypatch.setattr(ingest_mod.ai_api, "complete_structured", lambda *a, **k: {"operations": []})
    ingest_source("claude-code", vault_path=tmp_path, raw_root=root,
                  watermark_path=state, ai_env=None, today="2026-06-15")
    assert load_watermark("claude-code", path=state) is not None
```

- [ ] **Step 2:** `uv run pytest tests/test_wiki_ingest_checkpoint.py -v` → fail.
- [ ] **Step 3: implement** — `ingest_source(..., checkpoint_each: bool = False)`:
  - **max_mtime 갱신을 성공 경로로 이동**: 현재 루프가 try/except 밖에서 `max_mtime`을 갱신한다면, doc 처리가 예외 없이 끝난 경우에만 갱신하도록 try 블록 끝(또는 except에서 `continue`)으로 옮긴다. 예외 doc은 watermark 전진 X → 재실행 시 재시도.
  - 성공 후 `if checkpoint_each and not dry_run and max_mtime: save_watermark(source, max_mtime, path=watermark_path)`.
  - 끝에서 1회 저장 로직 유지(checkpoint_each=False 경로). checkpoint_each=True에서 마지막 중복 저장은 무해.
- [ ] **Step 4:** pass (2) + 기존 ingest/settled/checkpoint 회귀. **Step 5:** commit `feat(wiki): add per-doc checkpoint to ingest (resumable)`.

---

## Task 2: 배치 백필 루프 (wiki/backfill.py)

**Files:** Create `src/synapse_memory/wiki/backfill.py`; Test `tests/test_wiki_backfill.py`.

- [ ] **Step 1: failing test**
```python
# tests/test_wiki_backfill.py
from __future__ import annotations
from pathlib import Path
import synapse_memory.wiki.backfill as bf
from synapse_memory.wiki.backfill import run_backfill
from synapse_memory.wiki.ingest import IngestResult


def test_backfill_loops_until_drained(tmp_path, monkeypatch) -> None:
    seq = [
        IngestResult(source="claude-code", docs_processed=2, pages_written=["a", "b"]),
        IngestResult(source="claude-code", docs_processed=2, pages_written=["c"]),
        IngestResult(source="claude-code", docs_processed=0),
    ]
    calls = {"n": 0}
    def fake_ingest(source, **kw):
        assert kw.get("checkpoint_each") is True
        assert kw.get("min_age_seconds") is None
        assert kw.get("limit") == 2
        r = seq[calls["n"]]; calls["n"] += 1
        return r
    monkeypatch.setattr(bf, "ingest_source", fake_ingest)
    result = run_backfill(source="claude-code", vault_path=tmp_path, batch_size=2)
    assert result.batches == 3
    assert result.docs_processed == 4
    assert set(result.pages_written) == {"a", "b", "c"}


def test_backfill_respects_max_batches(tmp_path, monkeypatch) -> None:
    monkeypatch.setattr(bf, "ingest_source",
        lambda source, **kw: IngestResult(source=source, docs_processed=kw["limit"], pages_written=["x"]))
    result = run_backfill(source="claude-code", vault_path=tmp_path, batch_size=5, max_batches=2)
    assert result.batches == 2
```

- [ ] **Step 2:** fail. **Step 3: implement** `wiki/backfill.py`:
  - `from synapse_memory.wiki.ingest import ingest_source` (모듈 레벨).
  - `@dataclass class BackfillResult: source: str; batches: int = 0; docs_processed: int = 0; pages_written: list[str] = field(default_factory=list); errors: list[str] = field(default_factory=list)`.
  - `run_backfill(*, source="claude-code", vault_path=None, ai_env=None, batch_size=20, max_batches=None, today=None) -> BackfillResult`: `result = BackfillResult(source=source)`; while True: `r = ingest_source(source, vault_path=vault_path, ai_env=ai_env, limit=batch_size, checkpoint_each=True, min_age_seconds=None, today=today)`; `result.batches += 1; result.docs_processed += r.docs_processed; result.pages_written.extend(r.pages_written); result.errors.extend(r.errors)`; if `r.docs_processed == 0`: break; if `max_batches is not None and result.batches >= max_batches`: break. 반환.
- [ ] **Step 4:** pass (2). **Step 5:** commit `feat(wiki): add resumable batched backfill loop`.

---

## Task 3: CLI `backfill` + 회귀

**Files:** Modify `src/synapse_memory/cli.py`, `wiki/__init__.py`; Test `tests/test_cli_backfill.py`.

- [ ] **Step 1: failing test**
```python
# tests/test_cli_backfill.py
import synapse_memory.cli as cli
from synapse_memory.wiki.backfill import BackfillResult


def test_cli_backfill(monkeypatch, capsys):
    captured = {}
    def fake(**kw):
        captured.update(kw)
        return BackfillResult(source="claude-code", batches=3, docs_processed=10, pages_written=["a"])
    monkeypatch.setattr(cli, "run_backfill", fake)
    rc = cli.main(["backfill", "--source", "claude-code", "--batch-size", "5"])
    assert rc == 0
    assert captured["batch_size"] == 5
    out = capsys.readouterr().out
    assert "10" in out and "3" in out
```

- [ ] **Step 2:** fail. **Step 3: implement** — cli.py 상단 `from synapse_memory.wiki.backfill import run_backfill`. `build_parser`에 `backfill` 서브파서: `--source`(default "claude-code", choices=["claude-code"]), `--batch-size`(type=int, default=20), `--max-batches`(type=int, default=None). `cmd_backfill(args)`: `r = run_backfill(source=args.source, batch_size=args.batch_size, max_batches=args.max_batches)`; `print(f"backfill {r.source}: {r.batches} batches, {r.docs_processed} docs, {len(r.pages_written)} pages")`; if `r.errors`: `print(f"  errors: {len(r.errors)}")`; return `1 if r.errors else 0`.
  - `wiki/__init__.py`: export `run_backfill`, `BackfillResult`.
- [ ] **Step 4:** `uv run pytest -q` 전체 통과 + `ruff check src/synapse_memory/wiki src/synapse_memory/cli.py tests/test_wiki_backfill.py tests/test_wiki_ingest_checkpoint.py tests/test_cli_backfill.py` clean. **Step 5:** commit `feat(cli): add 'backfill' subcommand (resumable full-history rebuild)`.

---

## Self-Review
- **Spec coverage:** spec 019 §4 R5 — 빈 vault 재구축(전체 raw, 변환기 없음), 재개 가능 청크 ingest(per-doc 체크포인트 Task 1 + 배치 루프 Task 2), 유휴필터 끔. 멀티소스 제외(범위 메모).
- **재사용:** P1 `ingest_source`를 그대로 배치 호출 — 새 통합/적용 로직 없음. 재개는 watermark 전진으로.
- **테스트:** `ingest_source` 주입/mock으로 배치 루프·소진·max_batches·체크포인트 결정적 검증. 실제 LLM 없음.
- **Type consistency:** `ingest_source(..., checkpoint_each: bool=False)`, `run_backfill(*, source, vault_path, ai_env, batch_size, max_batches, today) -> BackfillResult(source, batches, docs_processed, pages_written, errors)`가 전 태스크 일관. P1 `IngestResult` 재사용.
- **재개 정확성:** 예외 doc은 watermark 전진 X → 재실행 시 재시도(Task 1에서 max_mtime을 성공 경로에서만 갱신).
