# P4 — Lint (구조 자동수정 + 사람 검토 큐) Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: superpowers:subagent-driven-development 또는 executing-plans. TDD, 태스크별 커밋.

**Goal:** wiki 신선도를 유지한다 — 구조 결함(끊긴 역링크, 죽은 링크, 고아)은 **자동 수정**하고, 진위 판단이 필요한 것(낡음 의심, 병합 후보)은 `index.md`의 **검토 큐**에 쌓는다(R3: "구조는 자동, 진실은 사람"). 전부 순수 Python — LLM 불필요.

**Architecture:** `list_pages`로 전 타입 페이지를 모아 그래프 분석. 자동 수정은 P0 `with_related`/`save_page`로 멱등 적용. `index.md`는 마커 사이 영역만 재생성(사용자 편집 보존). 분석기는 `list[WikiPage]` 입력의 순수 함수 → 결정적 단위테스트.

**Tech Stack:** P0 `wiki/page.py`(`list_pages`/`with_related`/`save_page`/`extract_wikilinks`), P1 `wiki/log.py`, `difflib`, `datetime`, `pytest`.

---

## 범위 메모

- **자동 수정(구조)**: 끊긴 역링크 보강, 죽은 `[[링크]]` 정리, 고아 탐지(→index.md). frontmatter는 P0 parse/serialize가 save 시 정규화하므로 별도 작업 없음.
- **검토 큐(진실, 자동수정 안 함)**: 낡음 의심(updated 나이 ≥ 임계), 병합 후보(제목 유사도 ≥ 임계). `index.md`에 나열만.
- **제외**: LLM 기반 모순 검출(후속), 초기 백필(P5).
- **멱등성**: lint 재실행이 새 변경을 만들지 않아야 함.

---

## File Structure

- Create: `src/synapse_memory/wiki/lint.py` — 분석기 + `apply_structural_fixes` + 검토 큐 휴리스틱 + `run_lint` + `LintReport`.
- Create: `src/synapse_memory/wiki/index_md.py` — `render_index`/`write_index`(마커 보존).
- Modify: `src/synapse_memory/cli.py` — `lint` 서브커맨드(`--now`).
- Modify: `src/synapse_memory/wiki/__init__.py` — export.
- Test: `tests/test_wiki_lint_analyze.py`, `test_wiki_lint_fix.py`, `test_wiki_lint_review.py`, `test_wiki_index_md.py`, `test_cli_lint.py`.

---

## Task 1: 그래프 분석기 (lint.py 순수 함수)

**Files:** Create `src/synapse_memory/wiki/lint.py`; Test `tests/test_wiki_lint_analyze.py`.

- [ ] **Step 1: failing test**
```python
# tests/test_wiki_lint_analyze.py
"""구조 분석기 — 순수 함수 (디스크 불필요)."""
from __future__ import annotations

from synapse_memory.wiki.lint import find_broken_backlinks, find_dead_links, find_orphans
from synapse_memory.wiki.page import WikiPage


def _p(slug, related=()):
    return WikiPage(type="concept", slug=slug, title=slug.upper(), related=tuple(related))


def test_find_broken_backlinks() -> None:
    pages = [_p("a", ["[[b]]"]), _p("b")]
    broken = find_broken_backlinks(pages)
    assert ("b", "a") in [(s, t) for s, t in broken]  # b가 a로의 역링크 필요


def test_find_dead_links() -> None:
    pages = [_p("a", ["[[ghost]]", "[[b]]"]), _p("b")]
    dead = find_dead_links(pages)
    assert ("a", "ghost") in dead
    assert ("a", "b") not in dead


def test_find_orphans() -> None:
    pages = [_p("a", ["[[b]]"]), _p("b"), _p("lonely")]
    orphans = find_orphans(pages)
    assert "lonely" in orphans
    assert "b" not in orphans
    assert "a" not in orphans
```

- [ ] **Step 2:** `uv run pytest tests/test_wiki_lint_analyze.py -v` → fail.
- [ ] **Step 3: implement** `src/synapse_memory/wiki/lint.py` (분석기):
  - `_targets(page) -> list[str]`: `page.related`의 각 링크에서 `extract_wikilinks(link) or [link.strip("[]")]`로 slug 추출.
  - `find_broken_backlinks(pages) -> list[tuple[str, str]]`: A가 B를 링크하는데 B가 A를 링크 안 하면 `(B, A)`(B가 A로의 역링크 필요). B가 pages에 존재할 때만.
  - `find_dead_links(pages) -> list[tuple[str, str]]`: A의 링크 대상이 pages에 없으면 `(A, target)`.
  - `find_orphans(pages) -> list[str]`: 들어오는 링크 0 & 나가는 링크 0인 완전 고립 slug.
- [ ] **Step 4:** pass (3). **Step 5:** commit `feat(wiki): add lint graph analyzers (broken backlinks/dead links/orphans)`.

---

## Task 2: 구조 자동 수정 (apply_structural_fixes)

**Files:** Modify `src/synapse_memory/wiki/lint.py`; Test `tests/test_wiki_lint_fix.py`.

- [ ] **Step 1: failing test**
```python
# tests/test_wiki_lint_fix.py
from __future__ import annotations
from pathlib import Path
from synapse_memory.wiki.lint import apply_structural_fixes
from synapse_memory.wiki.page import WikiPage, load_page, save_page


def test_adds_missing_backlinks(tmp_path: Path) -> None:
    save_page(WikiPage(type="concept", slug="a", title="A", related=("[[b]]",)), vault_path=tmp_path)
    save_page(WikiPage(type="concept", slug="b", title="B"), vault_path=tmp_path)
    report = apply_structural_fixes(vault_path=tmp_path)
    assert report.backlinks_added >= 1
    assert "[[a]]" in load_page("concept", "b", vault_path=tmp_path).related


def test_removes_dead_links(tmp_path: Path) -> None:
    save_page(WikiPage(type="concept", slug="a", title="A", related=("[[ghost]]", "[[b]]")), vault_path=tmp_path)
    save_page(WikiPage(type="concept", slug="b", title="B"), vault_path=tmp_path)
    report = apply_structural_fixes(vault_path=tmp_path)
    a = load_page("concept", "a", vault_path=tmp_path)
    assert "[[ghost]]" not in a.related
    assert "[[b]]" in a.related
    assert report.dead_links_removed >= 1


def test_idempotent(tmp_path: Path) -> None:
    save_page(WikiPage(type="concept", slug="a", title="A", related=("[[b]]",)), vault_path=tmp_path)
    save_page(WikiPage(type="concept", slug="b", title="B"), vault_path=tmp_path)
    apply_structural_fixes(vault_path=tmp_path)
    second = apply_structural_fixes(vault_path=tmp_path)
    assert second.backlinks_added == 0
    assert second.dead_links_removed == 0
```

- [ ] **Step 2:** fail. **Step 3: implement**:
  - `@dataclass class LintReport: backlinks_added: int = 0; dead_links_removed: int = 0; orphans: list[str] = field(default_factory=list); review_items: list[dict] = field(default_factory=list)`.
  - `apply_structural_fixes(*, vault_path=None) -> LintReport`: 전 타입 `list_pages` 수집. ① dead link 제거: 각 페이지 related에서 죽은 대상 제거(`dataclasses.replace` + `save_page`), 카운트. ② broken backlink 보강: 누락 역링크 `with_related` + `save_page`, 카운트. (dead 먼저, backlink 다음.) 멱등.
- [ ] **Step 4:** pass (3). **Step 5:** commit `feat(wiki): add apply_structural_fixes (backlinks + dead-link cleanup, idempotent)`.

---

## Task 3: 검토 큐 휴리스틱 (stale/merge)

**Files:** Modify `src/synapse_memory/wiki/lint.py`; Test `tests/test_wiki_lint_review.py`.

- [ ] **Step 1: failing test**
```python
# tests/test_wiki_lint_review.py
from __future__ import annotations
from synapse_memory.wiki.lint import merge_candidates, stale_candidates
from synapse_memory.wiki.page import WikiPage


def _p(slug, title, updated=""):
    return WikiPage(type="concept", slug=slug, title=title, updated=updated)


def test_stale_by_age() -> None:
    pages = [_p("old", "Old", "2020-01-01"), _p("fresh", "Fresh", "2026-06-10")]
    stale = stale_candidates(pages, today="2026-06-15", max_age_days=180)
    assert "old" in stale and "fresh" not in stale


def test_stale_missing_updated_flagged() -> None:
    assert "noupd" in stale_candidates([_p("noupd", "NoUpd", "")], today="2026-06-15", max_age_days=180)


def test_merge_candidates_by_title_similarity() -> None:
    pages = [_p("rag", "RAG"), _p("rag-1", "RAG"), _p("other", "전혀 다른 주제")]
    pairs = merge_candidates(pages, threshold=0.9)
    flat = {frozenset(p) for p in pairs}
    assert frozenset({"rag", "rag-1"}) in flat
```

- [ ] **Step 2:** fail. **Step 3: implement**:
  - `stale_candidates(pages, *, today=None, max_age_days=180) -> list[str]`: type=="insight"는 skip. `updated` 없으면 flag; 있으면 `date.fromisoformat(updated) < today - max_age_days`면 flag. today 미지정→오늘.
  - `merge_candidates(pages, *, threshold=0.9) -> list[tuple[str, str]]`: 같은 type 페이지쌍 중 `difflib.SequenceMatcher(None, t1.lower(), t2.lower()).ratio() >= threshold`인 (slug1, slug2).
- [ ] **Step 4:** pass (3). **Step 5:** commit `feat(wiki): add stale + merge-candidate review heuristics`.

---

## Task 4: index.md 생성 (마커 보존)

**Files:** Create `src/synapse_memory/wiki/index_md.py`; Test `tests/test_wiki_index_md.py`.

- [ ] **Step 1: failing test**
```python
# tests/test_wiki_index_md.py
from __future__ import annotations
from pathlib import Path
from synapse_memory.wiki.index_md import (
    MARKER_END, MARKER_START, index_md_path, render_index, write_index,
)
from synapse_memory.wiki.page import WikiPage


def test_index_md_path(tmp_path: Path) -> None:
    assert index_md_path(vault_path=tmp_path) == tmp_path / "index.md"


def test_render_lists_pages_and_review() -> None:
    pages = [WikiPage(type="project", slug="sm", title="SM")]
    body = render_index(pages, orphans=["sm"], review_items=[{"kind": "stale", "slug": "sm"}])
    assert "[[sm]]" in body
    assert "project" in body.lower()
    assert "stale" in body
    assert MARKER_START in body and MARKER_END in body


def test_write_preserves_user_content_outside_markers(tmp_path: Path) -> None:
    p = tmp_path / "index.md"
    p.write_text(f"내 메모\n{MARKER_START}\nOLD\n{MARKER_END}\n꼬리말\n", encoding="utf-8")
    write_index([WikiPage(type="concept", slug="x", title="X")], orphans=[], review_items=[], vault_path=tmp_path)
    text = p.read_text(encoding="utf-8")
    assert "내 메모" in text and "꼬리말" in text
    assert "OLD" not in text
    assert "[[x]]" in text
```

- [ ] **Step 2:** fail. **Step 3: implement** `wiki/index_md.py`:
  - `MARKER_START = "<!-- SYNAPSE:INDEX:START -->"`, `MARKER_END = "<!-- SYNAPSE:INDEX:END -->"`.
  - `index_md_path(*, vault_path=None)`: vault 루트/`index.md` (`get_vault_path` 폴백, expanduser/resolve).
  - `render_index(pages, *, orphans, review_items) -> str`: 마커 사이 — 타입별 `- [[slug]] (title)` 목록, "## 고아 페이지", "## 검토 큐"(`- {kind}: [[slug]] ...`). MARKER_START/END 포함 블록 반환.
  - `write_index(pages, *, orphans, review_items, vault_path=None) -> Path`: 기존 파일이면 마커 사이만 교체(split on markers), 없으면 새 생성. 마커 밖 보존.
- [ ] **Step 4:** pass (3). **Step 5:** commit `feat(wiki): add index.md generator (marker-preserving catalog + review queue)`.

---

## Task 5: run_lint + CLI `lint` + 회귀

**Files:** Modify `src/synapse_memory/wiki/lint.py`, `cli.py`, `wiki/__init__.py`; Test `tests/test_cli_lint.py`.

- [ ] **Step 1: failing test**
```python
# tests/test_cli_lint.py
from __future__ import annotations
import synapse_memory.cli as cli
from synapse_memory.wiki.page import WikiPage, load_page, save_page


def test_run_lint_fixes_and_writes_index(tmp_path):
    save_page(WikiPage(type="concept", slug="a", title="A", related=("[[b]]",)), vault_path=tmp_path)
    save_page(WikiPage(type="concept", slug="b", title="B"), vault_path=tmp_path)
    from synapse_memory.wiki.lint import run_lint
    report = run_lint(vault_path=tmp_path, today="2026-06-15")
    assert report.backlinks_added >= 1
    assert (tmp_path / "index.md").exists()
    assert "[[a]]" in load_page("concept", "b", vault_path=tmp_path).related


def test_cli_lint_now(monkeypatch, capsys):
    from synapse_memory.wiki.lint import LintReport
    monkeypatch.setattr(cli, "run_lint", lambda **kw: LintReport(backlinks_added=2, dead_links_removed=1))
    assert cli.main(["lint", "--now"]) == 0
    assert "2" in capsys.readouterr().out
```

- [ ] **Step 2:** fail. **Step 3: implement**:
  - `run_lint(*, vault_path=None, today=None) -> LintReport`: `report = apply_structural_fixes(vault_path=vault_path)` → 수정 후 `pages = [모든 타입 list_pages]` → `orphans = find_orphans(pages)`, `stale = stale_candidates(pages, today=today)`, `merges = merge_candidates(pages)` → `review_items = [{"kind":"stale","slug":s} for s in stale] + [{"kind":"merge","slug":a,"other":b} for a,b in merges]` → `report.orphans = orphans; report.review_items = review_items` → `write_index(pages, orphans=orphans, review_items=review_items, vault_path=vault_path)` → `append_log(f"lint: +{report.backlinks_added} backlinks, -{report.dead_links_removed} dead, {len(review_items)} review", vault_path=vault_path)` → report 반환.
  - cli.py: 상단 `from synapse_memory.wiki.lint import run_lint`. `build_parser`에 `lint` 서브파서 + `--now`. `cmd_lint(args)`: `r = run_lint()`; `print(f"lint: +{r.backlinks_added} backlinks, -{r.dead_links_removed} dead links, {len(r.review_items)} review items")`; return 0.
- [ ] **Step 4:** `uv run pytest -q` 전체 통과 + `ruff check src/synapse_memory/wiki src/synapse_memory/cli.py tests/test_wiki_lint_*.py tests/test_wiki_index_md.py tests/test_cli_lint.py` clean. **Step 5:** commit `feat(cli): add 'lint --now' (structural auto-fix + review queue + index.md)`.

---

## Self-Review
- **Spec coverage:** spec 019 §4 LINT + R3 — 자동 수정(역링크/죽은링크/고아 Task 1·2), 사람 검토 큐(낡음/병합 Task 3 → index.md Task 4), `index.md` 카탈로그(§5), log 기록(Task 5). LLM 모순검출·백필(P5) 제외.
- **순수성/테스트:** 분석기는 순수 함수, 수정은 tmp_path 주입, 시간은 `today` 주입 → 결정적. 멱등성 테스트 포함.
- **Type consistency:** `find_broken_backlinks/find_dead_links(pages)->list[tuple[str,str]]`, `find_orphans(pages)->list[str]`, `LintReport(backlinks_added,dead_links_removed,orphans,review_items)`, `apply_structural_fixes(*,vault_path)`, `stale_candidates(pages,*,today,max_age_days)`, `merge_candidates(pages,*,threshold)`, `render_index/write_index(pages,*,orphans,review_items,...)`, `run_lint(*,vault_path,today)`가 전 태스크 일관. P0 `with_related`/`save_page`/`extract_wikilinks`, P1 `append_log` 재사용.
- **사용자 보존:** index.md는 마커 사이만 교체.
